// Package s3 is object storage on S3 and everything that speaks its API
// (MinIO, Cloudflare R2, Railway buckets): a storage.Bucket with presigned
// direct uploads and downloads, checked at boot.
package s3
