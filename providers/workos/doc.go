// Package workos plugs WorkOS into a gox service: the API client, AuthKit
// login and callback handlers, a session middleware that verifies and
// refreshes the sealed session cookie, bearer access tokens, organization
// switching and webhook verification.
package workos
