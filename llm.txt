# gox — build a Go service with one import

gox is an opinionated service framework. It chooses the logger, config loader, HTTP server, middleware, observability, health endpoints and lifecycle so you do not. A plain HTTP service imports only `github.com/guilhermebr/gox`. Heavy integrations are opted in by importing their package and passing `Enable()` to `gox.New`: `gox/postgres`, `gox/jobs`, `gox/jwt`, `gox/openapi`, `gox/web`, and third-party clients under `gox/providers/*` (`gox/providers/supabase`, `gox/providers/temporal`, `gox/providers/workos`). This file is complete: everything public is listed below as Go signatures.

Rules that always hold:
- `main()` is `gox.MustNew(name, options...)`, handler registration, then `a.Run()`. gox owns start order, config, health, shutdown.
- Handlers are plain `net/http`: `func(w http.ResponseWriter, r *http.Request)`, registered with Go 1.22 patterns (`"GET /invoices/{id}"`, `r.PathValue("id")`). `"GET /{$}"` is the exact root; `"GET /"` is a catch-all.
- Config comes from environment variables under one prefix, the uppercased service name: service `billing` reads `BILLING_HTTP_ADDR`, `BILLING_POSTGRES_URL`. `gox.WithConfigPrefix("")` makes them unprefixed. `--help` prints every variable.
- Errors: return `gox.NotFound("invoice %s", id)` style errors and render them with `gox.Error(w, r, err)` (JSON) or `web.Error(w, r, err)` (HTML or JSON). Never write status codes by hand for errors.
- Accessors for undeclared features panic with a message naming the option to add; read the message and add the option.
- Every service gets, without writing code for it: structured logs with request ids, traces and metrics, `/healthz` and `/readyz` on the public port, an admin server on `:9090` with `/metrics`, `/healthz`, `/readyz`, `/version`, `/debug/pprof/`, graceful shutdown on SIGTERM.

Install: `go get github.com/guilhermebr/gox` (root); `go get github.com/guilhermebr/gox/postgres` (and so on) per feature; HTML apps also `go get github.com/a-h/templ` and install the `templ` CLI at the same version. Go 1.26+.

# Canonical mains (copy these)

JSON API, one import:

```go
package main

import (
	"net/http"
	"os"

	"github.com/guilhermebr/gox"
)

func main() {
	a := gox.MustNew("hello", gox.HTTP())
	a.HandleFunc("GET /hello/{name}", func(w http.ResponseWriter, r *http.Request) {
		_ = gox.JSON(w, http.StatusOK, map[string]string{"hello": r.PathValue("name")})
	})
	if err := a.Run(); err != nil {
		os.Exit(1)
	}
}
```

JSON API with Postgres and one migration, two imports:

```go
package main

import (
	"errors"
	"net/http"
	"os"
	"time"

	"github.com/jackc/pgx/v5"

	"github.com/guilhermebr/gox"
	"github.com/guilhermebr/gox/postgres"

	"example.com/billing/migrations" // package migrations: //go:embed *.sql; var FS embed.FS
)

type Config struct {
	gox.BaseConfig
	InvoiceTTL time.Duration `conf:"default:24h"`
}

func main() {
	var cfg Config
	a := gox.MustNew("billing",
		gox.WithConfig(&cfg),
		gox.HTTP(),
		postgres.Enable(postgres.WithMigrations(migrations.FS)),
	)
	db := postgres.From(a)

	a.HandleFunc("GET /invoices/{id}", func(w http.ResponseWriter, r *http.Request) {
		var id string
		err := db.QueryRow(r.Context(), "SELECT id FROM invoices WHERE id = $1", r.PathValue("id")).Scan(&id)
		if errors.Is(err, pgx.ErrNoRows) {
			gox.Error(w, r, gox.NotFound("invoice %s", r.PathValue("id")))
			return
		}
		if err != nil {
			gox.Error(w, r, err)
			return
		}
		_ = gox.JSON(w, http.StatusOK, map[string]string{"id": id})
	})

	if err := a.Run(); err != nil {
		os.Exit(1)
	}
}
```

Server-rendered HTML app (templ), two imports plus templ. The form input struct lives in the views package so both `main` and the templ component can use it. Run `go get github.com/a-h/templ` in the service (the CLI version must match `gox/web`'s pin) and `templ generate` before building; set `<PREFIX>_WEB_SESSION_SECRET` (32+ bytes) in production:

```go
package main

import (
	"net/http"
	"os"

	"github.com/guilhermebr/gox"
	"github.com/guilhermebr/gox/web"

	"example.com/shop/internal/guestbook/views" // templ components: views.Home, views.SignForm, views.SignInput
	"example.com/shop/static"                   // package static: //go:embed css js; var FS embed.FS
	"example.com/shop/web/layout"               // templ layout: layout.Layout
)

func main() {
	a := gox.MustNew("shop",
		gox.HTTP(),
		web.Enable(web.WithStatic(static.FS), web.WithSessions(), web.WithLayout(layout.Layout)),
	)
	a.HandleFunc("GET /{$}", func(w http.ResponseWriter, r *http.Request) {
		web.PageFrom(r).Title = "Home" // set Title before Render; the layout prints it
		_ = web.Render(w, r, views.Home())
	})
	a.HandleFunc("GET /sign", func(w http.ResponseWriter, r *http.Request) {
		_ = web.Render(w, r, views.SignForm(web.PageFrom(r), views.SignInput{}, nil))
	})
	a.HandleFunc("POST /sign", func(w http.ResponseWriter, r *http.Request) {
		in, ferrs, err := web.Form[views.SignInput](r)
		if err != nil {
			web.Error(w, r, err)
			return
		}
		if ferrs != nil {
			_ = web.RenderStatus(w, r, http.StatusUnprocessableEntity, views.SignForm(web.PageFrom(r), in, ferrs))
			return
		}
		web.AddFlash(w, r, "success", "Thanks, "+in.Name)
		web.Redirect(w, r, "/")
	})
	if err := a.Run(); err != nil {
		os.Exit(1)
	}
}
```

A templ layout (`web/layout/layout.templ`) receives the page context and the body:

```templ
package layout

import "github.com/guilhermebr/gox/web"

func Layout(page *web.Page, body templ.Component) templ.Component { return shell(page, body) }

templ shell(page *web.Page, body templ.Component) {
	<!DOCTYPE html>
	<html lang="en">
		<head><meta charset="utf-8"/><title>{ page.Title }</title>@web.CSRFMeta(page)<link rel="stylesheet" href={ page.Asset("css/app.css") }/></head>
		<body>
			for _, f := range page.Flashes { <div class={ "flash", "flash-" + f.Kind }>{ f.Message }</div> }
			<main>@body</main>
		</body>
	</html>
}
```

A form component (`internal/guestbook/views/sign.templ`) declares the input struct and takes the page (for the CSRF field), the input and the field errors keyed by form field name:

```templ
package views

import "github.com/guilhermebr/gox/web"

type SignInput struct {
	Name string `form:"name,required,min=2,max=40"`
}

templ SignForm(page *web.Page, in SignInput, errs web.FieldErrors) {
	<form method="post" action="/sign">
		@web.CSRFField(page)
		<input name="name" value={ in.Name }/>
		if msg, ok := errs["name"]; ok { <span class="field-error">{ msg }</span> }
		<button type="submit">Sign</button>
	</form>
}
```

A custom error page (`web/layout/errors.templ`) is a body fragment; gox wraps it in the layout. Set the title in the Go wrapper, since the layout prints it before the body renders:

```templ
package layout

import (
	"fmt"
	"github.com/guilhermebr/gox/web"
)

func ErrorPage(page *web.Page, status int, code, message string) templ.Component {
	page.Title = fmt.Sprint(status)
	return errorBody(status, message)
}

templ errorBody(status int, message string) {
	<h1>{ fmt.Sprint(status) }</h1>
	<p>{ message }</p>
}
```

# Project layout (the only supported one)

```
cmd/<name>/main.go            # gox.MustNew + <feature>.Register(a) calls + a.Run()
internal/<feature>/handler.go # net/http handlers for one feature; exposes Register(a *gox.App)
internal/<feature>/views/     # templ components for that feature (HTML apps)
web/layout/*.templ            # layout(s) (HTML apps)
web/components/*.templ        # shared components (HTML apps)
migrations/migrations.go      # package migrations: //go:embed *.sql; var FS embed.FS
migrations/NNNN_name.up.sql   # golang-migrate files (and .down.sql)
static/static.go              # package static: //go:embed css js img; var FS embed.FS (HTML apps)
.env.example                  # every <PREFIX>_* variable with its default
Makefile                      # build, test, lint, generate (templ), run
```

`//go:embed` cannot reach outside its own directory, so embedded files live in a package of their own and `main` imports it: `postgres.WithMigrations(migrations.FS)`, `web.WithStatic(static.FS)`.

```go
// migrations/migrations.go
package migrations

import "embed"

//go:embed *.sql
var FS embed.FS
```

Start order is fixed by stage, not by option order: datastores (StageDatastore) → clients (StageClient) → your components (StageUser) → public HTTP server (StageServer) → admin server. Stop is the reverse. `/readyz` turns green only after every component started; it turns red first on shutdown.

# Testing

Handler tests build the app without ports and serve its mux:

```go
func newApp(t *testing.T) *gox.App {
	t.Helper()
	a, err := gox.New("billing",
		gox.WithoutAdminServer(),                                      // no :9090
		gox.WithLogger(slog.New(slog.NewTextHandler(io.Discard, nil))), // silent
		gox.HTTP(),
	)
	if err != nil {
		t.Fatal(err)
	}
	invoices.Register(a, deps...)
	return a
}

func TestGetInvoice(t *testing.T) {
	rec := httptest.NewRecorder()
	newApp(t).Mux().ServeHTTP(rec, httptest.NewRequest(http.MethodGet, "/invoices/nope", nil))
	// rec.Code == 404; body is the envelope {"code":"not_found",...}
}
```

Facts to rely on:
- `gox.New` reads the real environment; set variables with `t.Setenv("BILLING_X", ...)`. Only `HTTP()` needs nothing.
- `a.Mux()` is the bare mux: handler code, `gox.Error` and `gox.JSON` work; the middleware chain (request ids, timeout, envelope 404 for unmatched routes) is not applied. To test through the chain, run `a.RunContext(ctx)` in a goroutine on a free port (`t.Setenv("BILLING_HTTP_ADDR", "127.0.0.1:0")` is not enough; pick a free port first), wait for `a.Health().IsReady()`, and use `http.Get`.
- Postgres-backed handlers: pass dependencies through `Register(a, db)` (the feature's `Register(a *gox.App, deps...)` takes what its handlers need) so tests call `Register(a, nil)` for paths that never reach the database, and keep database tests behind `//go:build integration` with `DATABASE_URL`. `postgres.Enable()` in a test app requires `<PREFIX>_POSTGRES_URL` and pings at `Run`, not at `New`; `postgres.From(a)` is valid right after `New`.
- After importing a module directly for the first time (`github.com/jackc/pgx/v5` for `pgx.ErrNoRows`), run `go mod tidy` and commit `go.mod` and `go.sum`.
- gox/web pages: serve the mux the same way; `web.Render` needs the web middleware, so test HTML handlers through the chain (free port) or test the templ components directly with `component.Render(ctx, &buf)`.

## API: gox (root)

import "github.com/guilhermebr/gox"

```go
const CodeOK, CodeCanceled, CodeUnknown, CodeInvalidArgument, CodeDeadlineExceeded, CodeNotFound, CodeAlreadyExists, CodePermissionDenied, CodeResourceExhausted, CodeFailedPrecondition, CodeAborted, CodeOutOfRange, CodeUnimplemented, CodeInternal, CodeUnavailable, CodeDataLoss, CodeUnauthenticated  // Codes.
type App struct{ /* unexported */ }  // App is a built service: config loaded, logger ready, components constructed.
func MustNew(name string, opts ...Option) *App  // MustNew is New for main(): --help and --version print and exit 0, any other error prints to stderr and exits 1.
func New(name string, opts ...Option) (*App, error)  // New builds an App: it applies the options, loads and validates config in one pass, builds the logger, runs every component factory, and wires the admin server.
func (a *App) Add(cs ...lifecycle.Component)  // Add registers user components at StageUser after New and before Run.
func (a *App) Config() BaseConfig  // Config returns the framework configuration.
func (a *App) ConfigPrefix() string  // ConfigPrefix returns the environment prefix config was loaded under ("BILLING", or "" for unprefixed), so features can name variables in their own error messages.
func (a *App) HTTPClient() *http.Client  // HTTPClient returns the default outbound client.
func (a *App) HandleFunc(pattern string, h http.HandlerFunc)  // HandleFunc registers a handler on the public mux using Go 1.22 patterns such as "GET /invoices/{id}".
func (a *App) HasHTTP() bool  // HasHTTP reports whether HTTP() was declared.
func (a *App) HasHTTPClient() bool  // HasHTTPClient reports whether HTTPClient() was declared.
func (a *App) Health() *health.Registry  // Health returns the registry behind /healthz and /readyz.
func (a *App) Log() *slog.Logger  // Log returns the service logger.
func (a *App) MapError(err error) error  // MapError applies the registered mappers to err.
func (a *App) Mux() *http.ServeMux  // Mux returns the public ServeMux.
func (a *App) Name() string  // Name returns the service name given to New.
func (a *App) Run() error  // Run starts the app and blocks until SIGINT or SIGTERM, or until a component fails fatally.
func (a *App) RunContext(ctx context.Context) error  // RunContext is Run with a caller-supplied context: cancel it to shut down.
func (a *App) Value(key any) (any, bool)  // Value returns a value stored by a feature package's Enable option.
type BaseConfig = config.Base  // BaseConfig is the configuration every service has.
type Builder struct{ /* unexported */ }  // Builder is the extension API feature packages use inside their Enable() option.
func (b *Builder) Component(stage Stage, fn func(a *App) (lifecycle.Component, error))  // Component registers a factory that runs at the end of New, after config and logging are ready, in stage order.
func (b *Builder) ConfigSection(name string, dst any, declaredBy string)  // ConfigSection registers a feature's config struct.
func (b *Builder) ErrorRenderer(fn httpx.ErrorRenderer)  // ErrorRenderer registers a renderer for error responses (an HTML error page).
func (b *Builder) Finish(fn func(a *App) error)  // Finish registers a function that runs at the end of New, after every component factory.
func (b *Builder) Middleware(mw ...Middleware)  // Middleware adds middleware a feature package needs on every request.
func (b *Builder) Set(key, value any)  // Set stores a value feature packages expose through their From accessor.
func (b *Builder) Setup(fn func(a *App) error)  // Setup registers a function that runs at the end of New, after config and logging are ready and before any component factory.
type CORSConfig = middleware.CORSConfig  // CORSConfig configures WithCORS.
type Code = errors.Code  // Code classifies a coded error.
func CodeOf(err error) Code  // CodeOf returns the code of err through wrapping: OK for nil, Unknown for foreign errors.
type Envelope = errors.Envelope  // Envelope is the JSON error body every gox response uses.
type ErrorMapper = errors.Mapper  // ErrorMapper translates a service's own errors into coded errors at the boundary.
type ErrorRenderer = httpx.ErrorRenderer  // ErrorRenderer writes an error response in place of the JSON envelope; it returns false to decline a request.
type HTTPOption func(*httpOptions)  // HTTPOption configures the public HTTP server declared with HTTP().
func WithCORS(cfg CORSConfig) HTTPOption  // WithCORS enables CORS for the given configuration.
func WithSPA(cfg SPAConfig) HTTPOption  // WithSPA serves a single-page application from the same origin as the API: files from cfg.FS at their paths, index.html for every other GET so client-side routes deep-link, and the error envelope for unmatched requests under cfg.ServerPrefixes.
type Middleware = middleware.Middleware  // Middleware wraps an http.Handler.
type Option func(*Builder) error  // Option declares one thing the app is made of or one behaviour of it.
func Component(c lifecycle.Component) Option  // Component adds any user component at StageUser.
func HTTP(opts ...HTTPOption) Option  // HTTP declares the public HTTP server: a net/http ServeMux behind the default middleware chain, /healthz and /readyz on the public port, and 404/405 rendered as the error envelope.
func HTTPClient() Option  // HTTPClient declares the default outbound client: timeouts and pool limits from HTTP_CLIENT_* config, trace and request-id propagation, and "User-Agent: <service>/<version>".
func Periodic(name string, every time.Duration, fn func(context.Context) error) Option  // Periodic runs fn every interval as a component: staggered first run, panic recovery, errors logged, stopped with the app.
func WithAuth(mw Middleware) Option  // WithAuth fills the auth slot of the chain: it runs after the default middleware and before WithMiddleware, for every route including the health probes, so an auth middleware should let those through.
func WithConfig(dst any) Option  // WithConfig registers the service's own config struct, which must embed BaseConfig.
func WithConfigPrefix(p string) Option  // WithConfigPrefix sets the environment prefix.
func WithErrorMapper(m ErrorMapper) Option  // WithErrorMapper registers a mapper applied by gox.Error before rendering, so a service keeps its domain sentinels and maps them once.
func WithErrorRenderer(fn ErrorRenderer) Option  // WithErrorRenderer replaces the JSON envelope for every error response, from handlers (gox.Error) and from the framework (404, 405, panics, timeouts, body limits) alike.
func WithLogger(l *slog.Logger) Option  // WithLogger replaces the logger gox would build from config.
func WithMiddleware(mw ...Middleware) Option  // WithMiddleware appends middleware after the default chain (and after the auth slot), outermost first.
func WithShutdownTimeout(d time.Duration) Option  // WithShutdownTimeout sets the default per-component stop budget.
func WithVersion(v string) Option  // WithVersion sets the build version reported in logs and on /version.
func WithoutAdminServer() Option  // WithoutAdminServer disables the ops server.
type SPAConfig = httpserver.SPAConfig  // SPAConfig describes a single-page application served next to the API.
type Stage int  // Stage orders component start-up.
const StageDatastore, StageClient, StageUser, StageServer Stage  // Stages.
func Aborted(format string, args ...any) *errors.Error  // Aborted creates a 409 error: a concurrency conflict; the client may retry.
func AlreadyExists(format string, args ...any) *errors.Error  // AlreadyExists creates a 409 error: the resource already exists.
func Canceled(format string, args ...any) *errors.Error  // Canceled creates a 499 error: the client went away.
func DataLoss(format string, args ...any) *errors.Error  // DataLoss creates a 500 error.
func DeadlineExceeded(format string, args ...any) *errors.Error  // DeadlineExceeded creates a 504 error.
func Decode(r *http.Request, v any) error  // Decode reads a strict JSON body into v.
func Error(w http.ResponseWriter, r *http.Request, err error)  // Error renders err as the standard envelope: the app's error mappers are applied, the status comes from the error's code, the request id is stamped, and server-side failures are logged with their cause.
func FailedPrecondition(format string, args ...any) *errors.Error  // FailedPrecondition creates a 400 error: the system is not in the required state.
func HTTPStatus(err error) int  // HTTPStatus returns the HTTP status err renders as.
func Internal(format string, args ...any) *errors.Error  // Internal creates a 500 error with a public-safe message.
func InvalidArgument(format string, args ...any) *errors.Error  // InvalidArgument creates a 400 error: the request is malformed or fails validation.
func JSON(w http.ResponseWriter, status int, v any) error  // JSON writes v as JSON with the given status.
func LoadConfig(name string, dst any) error  // LoadConfig loads dst (which must embed BaseConfig) from the environment under the service's default prefix, without building an App.
func MustValue[T any](a *App, key any, accessor, option string) T  // MustValue is what feature accessors use: it returns the stored value or panics with the standard message naming the accessor that was called and the option that was missing, for example "gox: postgres.From called but postgres.Enable() was not passed to gox.New".
func NewError(code Code, msg string) *errors.Error  // NewError creates a coded error with a public-safe message.
func NotFound(format string, args ...any) *errors.Error  // NotFound creates a 404 error.
func OutOfRange(format string, args ...any) *errors.Error  // OutOfRange creates a 400 error.
func PermissionDenied(format string, args ...any) *errors.Error  // PermissionDenied creates a 403 error.
func ProblemJSON(w http.ResponseWriter, r *http.Request, status int, env Envelope) bool  // ProblemJSON is an ErrorRenderer writing RFC 9457 problem details (application/problem+json): title, status, detail, plus code, request_id and every detail of the error as extension members.
func ResourceExhausted(format string, args ...any) *errors.Error  // ResourceExhausted creates a 429 error.
func Unauthenticated(format string, args ...any) *errors.Error  // Unauthenticated creates a 401 error.
func Unavailable(format string, args ...any) *errors.Error  // Unavailable creates a 503 error: retry later.
func Unimplemented(format string, args ...any) *errors.Error  // Unimplemented creates a 501 error.
func Unknown(format string, args ...any) *errors.Error  // Unknown creates a 500 error of unknown cause.
func Value[T any](a *App, key any) (T, bool)  // Value is the typed form of App.Value.
func WrapError(cause error, code Code, msg string) *errors.Error  // WrapError keeps cause for errors.Is and logs while msg is what clients see.
```

## Config: gox (root)

<PREFIX>_SERVICE_NAME (string): service name; set by gox.New
<PREFIX>_VERSION (string): build version; set by gox.WithVersion
<PREFIX>_ENVIRONMENT (string, default development): development | staging | production
<PREFIX>_HTTP_ADDR (string): listen address; PORT is honored when unset
<PREFIX>_HTTP_READ_HEADER_TIMEOUT (duration, default 10s)
<PREFIX>_HTTP_READ_TIMEOUT (duration, default 30s)
<PREFIX>_HTTP_WRITE_TIMEOUT (duration, default 30s)
<PREFIX>_HTTP_IDLE_TIMEOUT (duration, default 120s)
<PREFIX>_HTTP_REQUEST_TIMEOUT (duration, default 30s): per-request deadline enforced by the middleware chain
<PREFIX>_HTTP_MAX_BODY_BYTES (int64, default 1048576): request body limit in bytes
<PREFIX>_ADMIN_ADDR (string, default :9090)
<PREFIX>_SHUTDOWN_TIMEOUT (duration, default 30s): budget for each component's Stop
<PREFIX>_SHUTDOWN_PRE_STOP_DELAY (duration, default 0s): wait after going not-ready before stopping
<PREFIX>_LOG_LEVEL (string, default info): debug | info | warn | error
<PREFIX>_LOG_FORMAT (string, default auto): auto | json | text
<PREFIX>_OTEL_ENABLED (string, default auto): auto | true | false; auto is on in production or when an endpoint is set
<PREFIX>_OTEL_ENDPOINT (string): OTLP collector host:port
<PREFIX>_OTEL_PROTOCOL (string, default grpc): grpc | http
<PREFIX>_OTEL_INSECURE (bool, default false): plaintext connection to the collector
<PREFIX>_HTTP_CLIENT_TIMEOUT (duration, default 30s)
<PREFIX>_HTTP_CLIENT_MAX_IDLE_CONNS (int, default 100)
<PREFIX>_HTTP_CLIENT_IDLE_CONN_TIMEOUT (duration, default 90s)

## API: postgres

import "github.com/guilhermebr/gox/postgres"

```go
type Config struct{ URL string; MaxConns int32; MinConns int32; MaxConnLifetime time.Duration; MaxConnIdleTime time.Duration; HealthCheckPeriod time.Duration; ConnectTimeout time.Duration; Migrate bool; MigrationsTable string }  // Config is the POSTGRES config section: BILLING_POSTGRES_URL and friends under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks the section after loading.
type MigrateOption func(*migrateOptions)  // MigrateOption configures Migrate.
func WithMigrationsTable(name string) MigrateOption  // WithMigrationsTable stores the applied versions in another table than schema_migrations, for a database where another framework already owns that name.
type Option func(*options)  // Option configures Enable.
func WithMigrations(fsys fs.FS) Option  // WithMigrations runs the SQL migrations in fsys (golang-migrate layout: NNNN_name.up.sql / .down.sql, anywhere in the tree) at boot, before the app reports ready.
func Enable(opts ...Option) gox.Option  // Enable declares a Postgres pool: it registers the POSTGRES config section and a component at StageDatastore that connects and pings on Start (a lazy pool must never report healthy), runs migrations, exposes readiness, closes on Stop, and traces queries and pool stats.
func From(a *gox.App) *pgxpool.Pool  // From returns the pool.
func Migrate(ctx context.Context, url string, fsys fs.FS, opts ...MigrateOption) error  // Migrate applies every pending up migration in fsys to the database at url.
func New(ctx context.Context, prefix string) (*pgxpool.Pool, error)  // New builds a pool from the pre-framework variables (<PREFIX>_DATABASE_HOST, _PORT, _USER, _PASSWORD, _NAME, _SSLMODE, _POOL_MIN_SIZE, _POOL_MAX_SIZE).
func Tx(ctx context.Context, pool *pgxpool.Pool, fn func(tx pgx.Tx) error) (err error)  // Tx runs fn in a transaction: commit when fn returns nil, rollback otherwise (and on panic).
```

## Config: postgres (declared by postgres.Enable())

<PREFIX>_POSTGRES_URL (string, required, secret): postgres://user:pass@host:5432/db?sslmode=require
<PREFIX>_POSTGRES_MAX_CONNS (int32, default 10)
<PREFIX>_POSTGRES_MIN_CONNS (int32, default 2)
<PREFIX>_POSTGRES_MAX_CONN_LIFETIME (duration, default 1h): jittered so a fleet does not recycle in lockstep
<PREFIX>_POSTGRES_MAX_CONN_IDLE_TIME (duration, default 15m)
<PREFIX>_POSTGRES_HEALTH_CHECK_PERIOD (duration, default 1m)
<PREFIX>_POSTGRES_CONNECT_TIMEOUT (duration, default 10s)
<PREFIX>_POSTGRES_MIGRATE (bool, default true): run embedded migrations at boot (WithMigrations)
<PREFIX>_POSTGRES_MIGRATIONS_TABLE (string, default schema_migrations): table recording applied migrations; rename it when another framework owns schema_migrations

## API: jobs

import "github.com/guilhermebr/gox/jobs"

```go
type Config struct{ Work bool; MaxWorkers int; Migrate bool }  // Config is the JOBS config section: BILLING_JOBS_* under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks the section after loading.
func Enable(pool func(*gox.App) *pgxpool.Pool) gox.Option  // Enable declares the job queue on the pool that pool returns; pass postgres.From.
func From(a *gox.App) *river.Client[pgx.Tx]  // From returns the River client: Insert and InsertTx enqueue jobs, the latter inside the caller's transaction so a job exists only if the change that needs it committed.
func Register[T river.JobArgs](a *gox.App, w river.Worker[T])  // Register adds the worker for one kind of job.
func Schedule(a *gox.App, spec string, args river.JobArgs) error  // Schedule enqueues args on a schedule: a cron expression ("0 3 * * *", optionally prefixed with a zone, "CRON_TZ=America/New_York 0 0 * * *") or an interval ("@every 5m").
```

## Config: jobs (declared by jobs.Enable())

<PREFIX>_JOBS_WORK (bool, default true): work jobs in this process; false makes it insert-only (an API next to a separate worker)
<PREFIX>_JOBS_MAX_WORKERS (int, default 10): jobs worked at once in this process
<PREFIX>_JOBS_MIGRATE (bool, default true): create or upgrade River's tables at boot

## API: jwt

import "github.com/guilhermebr/gox/jwt"

```go
const RefreshWindow  // RefreshWindow is how close to expiry a token must be for RefreshToken to issue a new one.
var ErrInvalidToken error  // ErrInvalidToken indicates the token failed signature or validity checks.
var ErrInvalidClaims error  // ErrInvalidClaims indicates the token's claims could not be decoded.
var ErrNoSigningKey error  // ErrNoSigningKey is returned by GenerateToken on a verify-only service.
type Claims struct{ UserID string; Email string; AccountType string; jwt.RegisteredClaims; Raw map[string]any }  // Claims are the JWT claims issued and validated by Service.
func ClaimsFromContext(ctx context.Context) (*Claims, bool)  // ClaimsFromContext returns the claims Auth stored for this request.
func (c *Claims) UnmarshalJSON(b []byte) error  // UnmarshalJSON decodes the typed fields and keeps the whole claim set in Raw.
type Config struct{ SecretKey string; PublicKey string; JWKSURL string; PrivateKey string; Issuer string; Expiry time.Duration }  // Config is the JWT config section: BILLING_JWT_* under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks that exactly one algorithm is configured and that the PEM keys parse.
type Option func(*options)  // Option configures Enable.
func WithAuth() Option  // WithAuth protects every route with Auth, except /healthz and /readyz so platform probes keep working.
type Service struct{ /* unexported */ }  // Service signs and validates tokens with one algorithm: HS256 with a shared secret, or RS256 with an RSA key pair or an identity provider's key set.
func From(a *gox.App) *Service  // From returns the Service.
func NewFromConfig(cfg Config) (*Service, error)  // NewFromConfig builds a Service from a validated Config.
func NewHS256(secret []byte, issuer string, expiry time.Duration) *Service  // NewHS256 creates a Service that signs and verifies with a shared secret.
func NewJWKS(url, issuer string, client *http.Client) *Service  // NewJWKS creates a verify-only Service for tokens an identity provider signs with RS256: keys come from the provider's JWKS document at url, are cached, and follow rotation (an unknown key id triggers one rate-limited refetch).
func NewRS256(priv *rsa.PrivateKey, pub *rsa.PublicKey, issuer string, expiry time.Duration) *Service  // NewRS256 creates a Service that verifies with pub and, when priv is not nil, signs with it.
func (s *Service) GenerateToken(userID, email, accountType string) (string, error)  // GenerateToken issues a signed token for the given user.
func (s *Service) RefreshToken(tokenString string) (string, error)  // RefreshToken returns a fresh token when the supplied one is within RefreshWindow of expiry, otherwise the original token unchanged.
func (s *Service) ValidateToken(tokenString string) (*Claims, error)  // ValidateToken parses and verifies a token, returning its claims.
func Auth(svc *Service) gox.Middleware  // Auth is the middleware for gox.WithAuth: it validates the bearer token with svc and stores the claims for ClaimsFromContext.
func Enable(opts ...Option) gox.Option  // Enable registers the JWT config section and builds the Service from it.
```

## Config: jwt (declared by jwt.Enable())

<PREFIX>_JWT_SECRET_KEY (string, secret): HS256 secret; set exactly one of SECRET_KEY or PUBLIC_KEY or JWKS_URL
<PREFIX>_JWT_PUBLIC_KEY (string): PEM RSA public key for RS256 verification
<PREFIX>_JWT_JWKS_URL (string): identity provider key set for RS256 verification; keys are cached and follow rotation; set ISSUER to the provider's issuer
<PREFIX>_JWT_PRIVATE_KEY (string, secret): PEM RSA private key for RS256 signing; omit on verify-only services
<PREFIX>_JWT_ISSUER (string, default gox)
<PREFIX>_JWT_EXPIRY (duration, default 24h)

## API: openapi

import "github.com/guilhermebr/gox/openapi"

```go
type Violation struct{ Code string; Message string; Pointer string }  // Violation is one way a request breaks the contract.
func Enable(documents ...[]byte) gox.Option  // Enable validates every request that one of the documents describes: path, query, header and cookie parameters and the request body.
```

## API: supabase

import "github.com/guilhermebr/gox/providers/supabase"

```go
type Config struct{ URL string; Key string }  // Config is the SUPABASE config section: BILLING_SUPABASE_* under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks the URL is absolute.
func Enable() gox.Option  // Enable registers the SUPABASE config section and a client component at StageClient whose readiness probes the project's auth health endpoint.
func From(a *gox.App) *supabase.Client  // From returns the client.
func New(prefix string) (*supabase.Client, error)  // New creates a client from <PREFIX>_SUPABASE_URL and <PREFIX>_SUPABASE_KEY. Deprecated: pass supabase.Enable() to gox.New and read the client with supabase.From.
func NewFromConfig(cfg Config) (*supabase.Client, error)  // NewFromConfig creates a new Supabase client from a pre-loaded Config.
```

## Config: supabase (declared by supabase.Enable())

<PREFIX>_SUPABASE_URL (string, required): project URL such as https://xyz.supabase.co
<PREFIX>_SUPABASE_KEY (string, required, secret): anon or service role key

## API: temporal

import "github.com/guilhermebr/gox/providers/temporal"

```go
type Config struct{ Address string; Namespace string; APIKey string; TLS string; TLSCert string; TLSKey string; ConnectTimeout time.Duration; Work bool }  // Config is the TEMPORAL config section: BILLING_TEMPORAL_* under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks the section after loading.
func Enable() gox.Option  // Enable declares the Temporal client: it registers the TEMPORAL config section and a component that checks the connection at boot (an unreachable server fails the start, not the first workflow), exposes readiness, starts the workers registered with Worker, and on shutdown lets running activities finish within the shutdown budget before closing the client.
func From(a *gox.App) client.Client  // From returns the Temporal client, for starting, signalling and querying workflows.
func Worker(a *gox.App, taskQueue string, opts worker.Options) worker.Worker  // Worker returns the worker for a task queue, creating it on first use. Call it after New and before Run and register workflows and activities on the result; the app starts and stops it.
```

## Config: temporal (declared by temporal.Enable())

<PREFIX>_TEMPORAL_ADDRESS (string, default localhost:7233): host:port of the Temporal frontend
<PREFIX>_TEMPORAL_NAMESPACE (string, default default)
<PREFIX>_TEMPORAL_API_KEY (string, secret): Temporal Cloud API key; implies TLS
<PREFIX>_TEMPORAL_TLS (string, default auto): auto | true | false; auto is on with an API key or a client certificate
<PREFIX>_TEMPORAL_TLS_CERT (string): PEM client certificate for mTLS; needs TLS_KEY
<PREFIX>_TEMPORAL_TLS_KEY (string, secret): PEM client key for mTLS
<PREFIX>_TEMPORAL_CONNECT_TIMEOUT (duration, default 10s): how long the boot health check may take
<PREFIX>_TEMPORAL_WORK (bool, default true): run the registered workers in this process; false only starts workflows (an API next to a separate worker)

## API: workos

import "github.com/guilhermebr/gox/providers/workos"

```go
type Config struct{ APIKey string; ClientID string; RedirectURI string; CookiePassword string; CookieName string; CookieMaxAge time.Duration; SecureCookies string; BaseURL string; Issuer string; WebhookSecret string }  // Config is the WORKOS config section: BILLING_WORKOS_* under a service prefixed BILLING.
func (c *Config) Validate() error  // Validate checks the section after loading.
type Option func(*options)  // Option configures Enable.
func WithSessionCodec(c SessionCodec) Option  // WithSessionCodec replaces how the session cookie is sealed, for services that share the cookie with an application using another WorkOS SDK.
func WithSessions() Option  // WithSessions authenticates every request: the sealed session cookie, or a bearer access token when the Authorization header carries one.
type Session struct{ ID string; UserID string; OrganizationID string; Role string; Permissions []string; Entitlements []string; User *sdk.User; Impersonator *sdk.AuthenticateResponseImpersonator }  // Session is the authenticated caller of a request.
func SessionFrom(r *http.Request) (*Session, bool)  // SessionFrom returns the request's session; ok is false for anonymous requests.
type SessionCodec interface { Seal(data *sdk.SessionData, password string) (string, error) Unseal(sealed, password string) (*sdk.SessionData, error) }  // SessionCodec seals and unseals the session cookie value.
func Callback(a *gox.App) http.HandlerFunc  // Callback returns the handler for the redirect URI: it checks the state, exchanges the code, writes the session cookie and redirects to the path Login remembered (or "/").
func Enable(opts ...Option) gox.Option  // Enable registers the WORKOS config section and builds the API client.
func From(a *gox.App) *sdk.Client  // From returns the WorkOS API client.
func Login(a *gox.App) http.HandlerFunc  // Login returns the handler that starts an AuthKit sign-in: it redirects to the hosted login and remembers ?return_to= (local paths only) for Callback.
func Logout(a *gox.App) http.HandlerFunc  // Logout returns the handler that clears the session cookie and redirects to the WorkOS logout URL, which ends the session there and returns to ?return_to= when the application configured it as a logout redirect.
func RequireSession(next http.HandlerFunc) http.HandlerFunc  // RequireSession wraps a handler so anonymous requests get a 401.
func SwitchOrganization(w http.ResponseWriter, r *http.Request, organizationID string) error  // SwitchOrganization re-grants the request's session into another organization the user belongs to and rotates the cookie.
func VerifyWebhook(a *gox.App, r *http.Request) (*sdk.EventSchema, error)  // VerifyWebhook reads the request body, checks the WorkOS-Signature header against WORKOS_WEBHOOK_SECRET and returns the event.
```

## Config: workos (declared by workos.Enable())

<PREFIX>_WORKOS_API_KEY (string, required, secret): WorkOS API key (sk_...)
<PREFIX>_WORKOS_CLIENT_ID (string, required): WorkOS client id (client_...)
<PREFIX>_WORKOS_REDIRECT_URI (string): absolute URL of the route serving workos.Callback; required by workos.Login
<PREFIX>_WORKOS_COOKIE_PASSWORD (string, secret): key that seals the session cookie; at least 32 bytes; required by WithSessions
<PREFIX>_WORKOS_COOKIE_NAME (string, default wos_session)
<PREFIX>_WORKOS_COOKIE_MAX_AGE (duration, default 720h)
<PREFIX>_WORKOS_SECURE_COOKIES (string, default auto): auto | true | false; auto is on in production or behind X-Forwarded-Proto https
<PREFIX>_WORKOS_BASE_URL (string): API base URL; leave empty for api.workos.com (set it for an emulator)
<PREFIX>_WORKOS_ISSUER (string, default https://api.workos.com/): trusted access-token issuer; change it for a custom auth domain or an emulator
<PREFIX>_WORKOS_WEBHOOK_SECRET (string, secret): signing secret of the webhook endpoint; required by workos.VerifyWebhook

## API: web

import "github.com/guilhermebr/gox/web"

```go
const FieldCSRF  // FieldCSRF ("_csrf") is the hidden form field regular forms send the token in; a missing or wrong token is a 403.
const HeaderCSRF  // HeaderCSRF ("X-CSRF-Token") is the header htmx and fetch callers send the token in.
type API struct{ /* unexported */ }  // API calls the service's own JSON API as the signed-in user: the session token goes out as a bearer, request ids and trace context propagate, and a gox error envelope in a response becomes a coded error, so web.Error renders a backend 404 as a 404 page.
func APIFrom(r *http.Request) *API  // APIFrom returns the request's backend client.
func (a *API) Delete(ctx context.Context, path string) error  // Delete issues a DELETE.
func (a *API) Get(ctx context.Context, path string, out any) error  // Get decodes the JSON at path into out (nil to ignore the body).
func (a *API) Post(ctx context.Context, path string, in, out any) error  // Post sends in as JSON and decodes the response into out.
func (a *API) Put(ctx context.Context, path string, in, out any) error  // Put sends in as JSON and decodes the response into out.
type Config struct{ SessionSecret string; SessionName string; SessionMaxAge time.Duration; SecureCookies string; StaticPrefix string; BackendURL string }  // Config is the WEB config section: <PREFIX>_WEB_*.
func (c *Config) Validate() error  // Validate checks the section.
type ErrorPage func(page *Page, status int, code, message string) templ.Component  // ErrorPage renders an error for browser requests: status is the HTTP status, code the envelope code ("not_found"), message the public text.
type FieldErrors map[string]string  // FieldErrors maps a form field to its validation message, ready to be rendered next to the field.
func Form[T any](r *http.Request) (T, FieldErrors, error)  // Form decodes an application/x-www-form-urlencoded or multipart body into T and validates it from struct tags: Email string `form:"email,required"` Age int `form:"age,min=18,max=120"` Note string `form:"note,max=280"` // max on strings is a length Tags []string `form:"tags"` Wait time.Duration `form:"wait"` Agree bool `form:"agree,required"` // required means checked Validation failures come back as FieldErrors keyed by the form tag name ("email", not "Email") with nil error; messages are "required", "must be at least N characters", "must be at most N characters", "must be at least N", "must be at most N", "must be a whole number", "must be a number" and "must be a duration such as 30s or 5m".
func (e FieldErrors) Error() string  // Error implements error so FieldErrors can travel as one.
type Flash struct{ Kind string; Message string }  // Flash is a one-shot message shown on the next page: "success", "error", "info" and so on are the conventional kinds.
type Layout func(page *Page, body templ.Component) templ.Component  // Layout wraps a page body.
type Option func(*options)  // Option configures Enable.
func WithBackend() Option  // WithBackend enables APIFrom: a per-request client for the service's own API at WEB_BACKEND_URL, authenticated with the session token.
func WithCSRFExempt(prefixes ...string) Option  // WithCSRFExempt skips the CSRF check for paths with these prefixes, for endpoints that third parties call with their own authentication (webhooks).
func WithErrorPage(p ErrorPage) Option  // WithErrorPage replaces the default error page.
func WithLayout(l Layout) Option  // WithLayout sets the layout Render wraps pages in.
func WithSessionUser(fn func(r *http.Request, s *Session) (any, error)) Option  // WithSessionUser resolves Page.User once per request from the session (typically by calling the backend with APIFrom).
func WithSessions() Option  // WithSessions enables the encrypted cookie session (SessionFrom) and CSRF checks on every state-changing request.
func WithStatic(fsys fs.FS) Option  // WithStatic serves fsys under WEB_STATIC_PREFIX (default /static/) with content-hashed URLs in production; Page.Asset resolves names.
type Page struct{ Title string; Path string; User any; Flashes []Flash; CSRFToken string; Nonce string; Data map[string]any; Request *http.Request }  // Page is the per-request context every layout receives.
func PageFrom(r *http.Request) *Page  // PageFrom returns the request's Page, building it on first use: flashes are taken, the CSRF token is ensured and the session user is resolved.
func (p *Page) Asset(name string) string  // Asset returns the URL for a static file: content-hashed in production, plain in development.
type Session struct{ /* unexported */ }  // Session is the per-request, cookie-backed session.
func SessionFrom(r *http.Request) *Session  // SessionFrom returns the request's session.
func (s *Session) Clear()  // Clear drops every value and expires the cookie: logout.
func (s *Session) Delete(key string)  // Delete removes a value.
func (s *Session) Get(key string) (string, bool)  // Get returns a value.
func (s *Session) IsAuthenticated() bool  // IsAuthenticated reports whether a token is stored.
func (s *Session) Set(key, value string)  // Set stores a value.
func (s *Session) SetToken(token string)  // SetToken stores the backend bearer token the API client sends.
func (s *Session) Token() string  // Token returns the backend bearer token stored with SetToken, or "".
func AddFlash(w http.ResponseWriter, r *http.Request, kind, message string)  // AddFlash queues a message for the next rendered page.
func CSRFField(page *Page) templ.Component  // CSRFField renders the hidden input regular forms include.
func CSRFMeta(page *Page) templ.Component  // CSRFMeta renders <meta name="csrf-token">; the scaffold's htmx config reads it into the X-CSRF-Token header on every request.
func Enable(opts ...Option) gox.Option  // Enable declares the web layer.
func Error(w http.ResponseWriter, r *http.Request, err error)  // Error renders err as an error page for requests that want HTML (htmx, or Accept preferring text/html) and as the JSON envelope otherwise, with the same status either way.
func IsHTMX(r *http.Request) bool  // IsHTMX reports whether the request was made by htmx.
func Partial(w http.ResponseWriter, r *http.Request, c templ.Component) error  // Partial writes c without the layout, whatever the request.
func Redirect(w http.ResponseWriter, r *http.Request, url string)  // Redirect sends the browser to url: an HX-Redirect for htmx requests, a 303 See Other otherwise (the PRG pattern after a form post).
func Render(w http.ResponseWriter, r *http.Request, c templ.Component) error  // Render writes c with status 200 inside the layout, or alone for htmx fragment requests.
func RenderStatus(w http.ResponseWriter, r *http.Request, status int, c templ.Component) error  // RenderStatus is Render with an explicit status (422 for a form that failed validation).
func RequireSession(next http.HandlerFunc) http.HandlerFunc  // RequireSession wraps a handler so requests without a session token are redirected to /login.
```

## Config: web (declared by web.Enable())

<PREFIX>_WEB_SESSION_SECRET (string, secret): key for the session cookie; at least 32 bytes; required by WithSessions
<PREFIX>_WEB_SESSION_NAME (string, default session)
<PREFIX>_WEB_SESSION_MAX_AGE (duration, default 720h)
<PREFIX>_WEB_SECURE_COOKIES (string, default auto): auto | true | false; auto is on in production or behind X-Forwarded-Proto https
<PREFIX>_WEB_STATIC_PREFIX (string, default /static/)
<PREFIX>_WEB_BACKEND_URL (string): base URL of the API this UI calls as the signed-in user; required by WithBackend

# Conventions and contracts

Error envelope (every error response, from handlers and from the framework alike):

```json
{"code": "not_found", "message": "invoice inv_1", "request_id": "8e03…", "details": {"field": "amount"}}
```

`gox.Envelope` fields: `Code string`, `Message string`, `RequestID string`, `Details map[string]any`. The constructors return `*errors.Error` (from `gox/pkg/errors`, no import needed) with these methods: `WithDetail(key string, value any) *Error` (adds to `details`), `WithHTTPStatus(status int) *Error`, `Code() Code`, `Message() string`, `Details() map[string]any`, `Error() string`, `Unwrap() error`. Check codes with `gox.CodeOf(err) == gox.CodeNotFound`, never by comparing error text.

Codes and HTTP statuses: `invalid_argument` 400, `unauthenticated` 401, `permission_denied` 403, `not_found` 404, `already_exists` 409, `aborted` 409, `failed_precondition` 400, `resource_exhausted` 429, `unimplemented` 501 (405 for a wrong method), `internal` 500, `unavailable` 503, `deadline_exceeded` 504, `canceled` 499. Errors that are not gox errors render as `internal` with the message "internal error"; their text never reaches a client. Map your own sentinels once with `gox.WithErrorMapper`.

Log keys (contract with dashboards): `request_id`, `trace_id`, `span_id`, `error`, `component`, `duration`. Access log fields: `method`, `path`, `route` (the mux pattern), `status`, `bytes`. Level by status: 5xx error, 4xx warn, else info. `/healthz` and `/readyz` are not logged.

Metrics (Prometheus on the admin `/metrics`): `http_server_request_duration_seconds` and `http_server_active_requests` by `http_request_method`, `http_route`, `http_response_status_code`; `db_client_connection_count{state}` and `db_client_connection_max` with `postgres.Enable()`; Go runtime and process metrics.

Middleware chain (fixed): error renderers → route capture → recovery → request id → tracing → metrics → logging → timeout (`HTTP_REQUEST_TIMEOUT`) → max bytes (`HTTP_MAX_BODY_BYTES`) → security headers → CORS (only `gox.WithCORS`) → cross-origin protection → feature middleware → auth (`gox.WithAuth`) → `gox.WithMiddleware` → mux.

Forms (gox/web): `GET` renders the form; `POST` decodes with `web.Form[T]`, re-renders the same component with status 422 on `FieldErrors`, and on success calls `web.AddFlash` then `web.Redirect` (303, or `HX-Redirect` for htmx). `FieldErrors` is keyed by the `form` tag name; messages are `required`, `must be at least N characters`, `must be at most N characters`, `must be at least N`, `must be at most N`, `must be a whole number`, `must be a number`, `must be a duration such as 30s or 5m`. CSRF is automatic with sessions and applies to form submissions (`application/x-www-form-urlencoded`, `multipart/form-data`, or no Content-Type): include `@web.CSRFField(page)` (hidden field `_csrf`) in forms and `@web.CSRFMeta(page)` in the layout head (`X-CSRF-Token` header for htmx and fetch); a missing or wrong token is a 403. JSON requests (`Content-Type: application/json`) are never checked, so a JSON API and HTML pages coexist in one service and curl works; `web.WithCSRFExempt("/webhooks/")` skips prefixes that third parties call. Rejections by any middleware log the real route.

Cross-origin protection (always on): a state-changing request (`POST`, `PUT`, `PATCH`, `DELETE`) that a browser sends from another origin is a 403 `permission_denied`, decided from `Sec-Fetch-Site` and `Origin` with no token. Requests without those headers (curl, webhooks, server-to-server) and safe methods pass; origins in `gox.WithCORS` are trusted. A single-page app on the same origin needs nothing else.

Other error shapes: `gox.WithErrorRenderer(gox.ProblemJSON)` replaces the envelope with RFC 9457 `application/problem+json` for every error, framework rejections included (`title`, `status`, `detail`, `code`, `request_id`, and each `WithDetail` entry as a top-level member). Pass your own `gox.ErrorRenderer` to pick a shape by path; return false to fall back to the envelope.

Single-page apps: `gox.HTTP(gox.WithSPA(gox.SPAConfig{FS: dist.FS, ServerPrefixes: []string{"/api/"}}))` serves the built app from the API's origin: files at their paths (`/assets/` cached forever), `index.html` for every other `GET` so client routes deep-link, the error envelope for unmatched requests under the server prefixes. `SPAConfig.Head` injects per-request tags before `</head>`. Registered routes always win.

HTML errors (gox/web): `web.Enable` registers an error renderer, so `web.Error`, panics, timeouts, 404/405 and CSRF failures render the error page for requests that want HTML (htmx requests, or `Accept` preferring `text/html`) and the JSON envelope for everything else, with the same status either way. The `ErrorPage` function returns a body fragment; gox wraps it in the layout and `page` is never nil. Set `web.PageFrom(r).Title` before `web.Render`, in Go: templ components render lazily and the layout prints the title before the body.

Never:
- Never add a router (chi, gin, echo), an ORM, or a DI framework. `net/http` mux, pgx, explicit options.
- Never call `http.Error` or write error JSON by hand; use `gox.Error` / `web.Error`.
- Never read env vars directly; add fields to your config struct (`gox.WithConfig`).
- Never start goroutines that outlive a request outside a component or `gox.Periodic`; they would not be stopped on shutdown.
- Never mutate the shared HTTP client; derive one per request (`web.APIFrom`, `httpclient.WithBearer`).
- Never import `github.com/guilhermebr/gox/pkg/...` from a service unless you are writing a feature package; the root re-exports what services need.
- Never put datastore settings in `BaseConfig`; they are feature sections (`<PREFIX>_POSTGRES_URL`).

